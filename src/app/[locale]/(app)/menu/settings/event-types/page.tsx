import EventTypesScreen from '../../_components/EventTypesScreen';

// Manage event-type presets (event_types table, shared migration 006):
// owner or settings.manage_business only.
export default function EventTypesPage() {
  return <EventTypesScreen />;
}
