import MembersScreen from '../_components/MembersScreen';

// Members (§4.4, owner only): status chips, add by email + display name +
// permission matrix (shared/permissions/permissions-schema.json), edit/revoke.
export default function MembersPage() {
  return <MembersScreen />;
}
